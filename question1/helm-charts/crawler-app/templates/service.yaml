apiVersion: v1
kind: Service
metadata:
  name: {{ include "crawler-app.fullname" . }}
  labels:
    {{- include "crawler-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
    - port: 8090
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "crawler-app.selectorLabels" . | nindent 4 }}
